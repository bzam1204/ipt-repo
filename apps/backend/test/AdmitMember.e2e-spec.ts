import {axios} from "../src/infra/AxiosAdapter";
import {cpfGenerator} from "../src/infra/cpf-generator";

describe('Account', () => {

	it('deve admitir um novo membro', async () => {
		// const input = {
		// 	sex:
		// 	cpf: cpfGenerator(),
		// 	email: crypto.randomUUID() + "johndoe@gmail.com",
		// 	password: "P@ssw0rd",
		// 	lastName: 'Doe',
		// 	firstName: 'John'
		// };
		// const response = await axios.post('account/create', input);
		// expect(response.status).toBe(204);
	});

});
